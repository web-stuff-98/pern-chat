export interface IDimensions { width: number, height: number }
export interface IPosition { top: number, left: number }
export interface IResponseMessage { message: string, pending: boolean, error: boolean }